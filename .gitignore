# OS
.DS_Store

# Local tooling / planning (not tracked)
.agent/
.claude/

# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.next/
out/
.turbo/

# Cloudflare / Wrangler
.wrangler/
.dev.vars
worker-configuration.d.ts

# Env / secrets
.env
.env.*
!.env.example

# Local HTTPS (E05a D5 / task 007)
# `next dev --experimental-https` mints a locally-trusted cert into
# apps/web/certificates/. That directory holds a PRIVATE KEY — never commit it.
certificates/

# Drizzle
# (migrations under drizzle/ ARE tracked; only ignore local journal noise if any)

# Logs & misc
*.log
npm-debug.log*
pnpm-debug.log*
coverage/
.vercel/
*.tsbuildinfo

# Playwright
/test-results/
/playwright-report/
/.playwright/
**/test-results/
**/playwright-report/
**/.playwright/

# Playwright MCP scratch
.playwright-mcp/

# Design handoff bundles (local reference only)
*handoff.zip
