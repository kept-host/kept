// @kept/web — Next.js control plane. App scaffolded in task 003.
// Placeholder so the workspace type-checks/lints cleanly.
import { SHARED_PACKAGE } from "@kept/shared";

export const WEB_APP = `${SHARED_PACKAGE}:web` as const;
